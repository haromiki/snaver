Planned StatisticsModal navigation implementation
사용자가 순위 통계 네비게이션 구현을 원합니다. 앞서 분석한 바에 따르면:

StatisticsModal.tsx를 수정해서 props에 products 배열과 현재 인덱스, 네비게이션 콜백을 추가해야 합니다.
ProductTable.tsx에서 모달을 열 때 제품 목록과 현재 인덱스를 전달해야 합니다.
상단에 이전/다음 버튼과 현재 위치 표시를 추가해야 합니다.
먼저 StatisticsModal.tsx를 수정하고, 그 다음에 ProductTable.tsx를 수정하겠습니다.

순위 통계 네비게이션을 구현하겠습니다!


Created task list
StatisticsModal Props 확장 (products 배열, currentIndex, onNavigate 추가)
모달 상단에 네비게이션 바 UI 구현 (이전/다음 버튼, 위치 표시)
ProductTable에서 제품 목록과 인덱스 전달 로직 구현
키보드 단축키 지원 (좌우 화살표) 및 경계 처리
테스트 및 사용자 경험 개선

Planned modal header navigation addition
네비게이션 바를 모달 헤더 부분에 추가해야 합니다. 현재 모달의 헤더는 424-439 라인에 있는데, 이걸 수정해서 네비게이션 바를 추가하겠습니다.

현재 헤더는:

<div className="px-4 py-3 border-b border-gray-200 dark:border-gray-700 flex items-center justify-between">
  <div>
    <h3 className="text-base font-semibold text-gray-900 dark:text-gray-100">순위 통계</h3>
    <p className="text-xs text-gray-500 dark:text-gray-400">
      {product?.productName || `제품 #${productId}`}
      {product?.keyword && ` - ${product.keyword}`}
    </p>
  </div>
  <button onClick={onClose}>
    <i className="fas fa-times text-xl"></i>
  </button>
</div>
이걸 수정해서 네비게이션 바를 추가하겠습니다.


Edited client/src/components/StatisticsModal.tsx
-14
+52
-14
+52
  return (
    <div className="fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center p-2" data-testid="statistics-modal">
      <div className="bg-white dark:bg-gray-900 rounded-lg shadow-xl w-[80vw] max-w-[850px] max-h-[95vh] overflow-hidden border border-gray-200 dark:border-gray-700 flex flex-col">
        <div className="px-4 py-3 border-b border-gray-200 dark:border-gray-700 flex items-center justify-between">
          <div>
            <h3 className="text-base font-semibold text-gray-900 dark:text-gray-100">순위 통계</h3>
            <p className="text-xs text-gray-500 dark:text-gray-400">
              {product?.productName || `제품 #${productId}`}
              {product?.keyword && ` - ${product.keyword}`}
            </p>
        <div className="px-4 py-3 border-b border-gray-200 dark:border-gray-700">
          {/* 네비게이션 바 */}
          {products.length > 1 && onNavigate && (
            <div className="flex items-center justify-between mb-3">
              <button
                onClick={() => onNavigate(currentIndex - 1)}
StatisticsModal Props 확장 (products 배열, currentIndex, onNavigate 추가)
모달 상단에 네비게이션 바 UI 구현 (이전/다음 버튼, 위치 표시)
키보드 단축키 지원 (좌우 화살표) 및 경계 처리
ProductTable에서 제품 목록과 인덱스 전달 로직 구현
Task completed
44 minutes ago
Add navigation for viewing product statistics with keyboard shortcuts Implement navigation controls within the StatisticsModal component, allowing users to cycle through products using arrow keys.
